/*Esciribe una función llamada numDuplicados que reciba un
 string y retorne el número de caracteres que aparecen más de una vez*/
 console.log(numDuplicados("abcaa"))
 console.log(numDuplicados("abab"))
 console.log(numDuplicados("abc"))

function numDuplicados (letras){
    for (let i = 0; i < letras.length; i++){
        if(letras[i]===)
    }
}